import styled from 'styled-components';
import { page } from 'styles/variables';

const Page = styled.section`
  ${page}
  display: flex;
  align-items: center;
  justify-content: center;
`;

export default Page;
